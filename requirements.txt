# NumPy must stay <2: torch 2.3.x is compiled against the NumPy 1.x ABI and
# crashes at inference time ("Could not infer dtype of numpy.uint8") with NumPy 2.
numpy<2
fastapi==0.111.0
uvicorn[standard]==0.30.1
torch==2.3.1
torchvision==0.18.1
pillow==10.3.0
python-multipart==0.0.9
jinja2==3.1.4
